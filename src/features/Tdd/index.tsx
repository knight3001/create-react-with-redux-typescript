import React, {
  useState,
  useContext,
  useEffect,
  useCallback,
  useMemo,
} from "react";
import Title from "../../app/components/Title";

const Tdd = () => <Title>Clickable Fonts</Title>;

export default Tdd;
